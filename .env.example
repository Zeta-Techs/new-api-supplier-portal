# Base URL of your New-API backend.
# In dev, you can leave this blank and rely on Vite proxy (/api -> VITE_DEV_API_PROXY_TARGET).
VITE_API_BASE_URL=

# Dev-only: where Vite should proxy /api requests.
# This should point to the portal backend (BFF), not directly to new-api.
VITE_DEV_API_PROXY_TARGET=http://localhost:3001
