## Notes
This capability depends on:
- `supplier-billing` for per-supplier billing fields
- `channel-api-integration` for retrieving channel usage from new-api

The admin UI should show a simple table for suppliers and allow editing `settled_amount`.
